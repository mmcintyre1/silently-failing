---
interface Props {
  tag: string
}

const { tag } = Astro.props
---

<a
  href={`/tags/${tag}`}
  class="inline-block text-xs px-2 py-0.5 rounded bg-stone-100 text-stone-500 hover:bg-teal-50 hover:text-teal-700 transition-colors duration-150"
>
  {tag}
</a>
